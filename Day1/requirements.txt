# TDMMA 2026 -- Day 1: the transient sky, surveys and alert brokers
#
# One environment for the whole of Day 1 -- all four notebooks, not just the
# alert-broker session. From this directory:
#
#     python -m venv .venv
#     source .venv/bin/activate          # Windows: .venv\Scripts\activate
#     pip install -r requirements.txt
#
# Python 3.9 or newer.

# --- Running the notebooks at all -------------------------------------------
# Also supplies IPython and ipykernel; 01_Alert_Triage_Master.ipynb imports
# IPython.display.HTML directly.
jupyterlab>=4.0

# --- Core numerics, used by every notebook ----------------------------------
numpy>=1.21.0
pandas>=1.4.0
matplotlib>=3.5.0

# scipy.integrate.solve_ivp  -> TOV integration in Compact_Object_Physics
# scipy.signal.fftconvolve   -> PSF matching in Modern_Survey_Astronomy
scipy>=1.8.0

# --- Alert brokers (Alert_Brokers/) -----------------------------------------
alerce>=2.3.0,<3     # ZTF alert and light-curve queries via the ALeRCE broker
requests>=2.28.0     # HTTP layer beneath the broker client
pyarrow>=8.0.0       # reads data/reference_alerts.parquet, the offline failsafe

# --- Astronomy (Alert_Brokers/) ---------------------------------------------
# Time, SkyCoord, EarthLocation and AltAz, for the observability window.
# EarthLocation.of_site('ctio') downloads the observatory registry once and
# then caches it, so that step wants network the first time it runs.
astropy>=5.0

# --- Machine learning (Modern_Survey_Astronomy) -----------------------------
# RandomForestClassifier, train_test_split, accuracy_score, confusion_matrix --
# the real/bogus classifier on difference-image cutouts.
scikit-learn>=1.1
